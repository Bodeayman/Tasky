import 'package:flutter/material.dart';

class Finishedtaskspage extends StatelessWidget {
  const Finishedtaskspage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("This is the finished tasks page"),
    );
  }
}
