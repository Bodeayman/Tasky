import 'package:flutter/material.dart';

class Waitingtaskspage extends StatelessWidget {
  const Waitingtaskspage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Waiting tasks Page"),
    );
  }
}
